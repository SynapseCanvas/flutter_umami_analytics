name: flutter_umami_analytics_example
description: Example app for flutter_umami_analytics.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_umami_analytics:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
